<div id="shopify-section-textsection">
    {% for block in section.blocks %}
    {% case block.type %}
    {% when 'text' %}
    <div class="text" {{ block.shopify_attributes }}>
        <h3>{{ block.settings.text-text }}</h3>
    </div>
    {% endcase %}
    {% endfor %}
</div>

{% schema %}
{
    "name":"test-text",
    "blocks":[
       {
          "name":"text-text",
          "type":"text",
          "settings":[
             {
                "type":"text",
                "id":"text-test",
                "label":"text"
             }
          ]
       }
    ],
    "presets":[
       {
          "name":"test-preset"
       }
    ]
 }
{% endschema %}
